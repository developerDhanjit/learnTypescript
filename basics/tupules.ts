// Tupules : ordering the items inside the array 

let NewUser : [string, number, boolean];

NewUser = ["dhanjit", 101, true]
// NewUser = ["dhanjit", "101", true] not allowed 

let rgb: [number, number, number]

rgb = [244,255,255]
